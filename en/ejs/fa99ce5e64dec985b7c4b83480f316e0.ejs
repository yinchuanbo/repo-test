<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title></title>
  <meta name="description" content="  " />
  <meta name="keywords" content="" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport"
    content="width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=0,minimal-ui,viewport-fit=cover" />
  <link rel="icon" href="/dist/img/FS-favicon.ico" />
  <link rel="stylesheet" type="text/css" href="/dist/css/404.css" />
  <link rel="canonical" href="https://www.vidqu.ai/404.html" />
  <%- include('../patiles/head'); -%>

</head>

<body>
  <%- include('../patiles/header'); -%>

    <div class="main">
      <img src="/dist/img/404/banner.png" alt="" class="banner">
      <div class="text">
        <h1><%- noFondPage.h1 -%></h1>
        <div class="text1"><%- noFondPage.text1 -%></div>
        <div class="text2"><%- noFondPage.text2-%></div>
      </div>
    </div>

    <%- include('../patiles/footer'); -%>

</body>
<script>
  function setInsur() {
    let insurValue = "en_vidq_404";
    if (isSeo()) {
      insurValue = insurValue.replace("en", "enseo");
    }
    return insurValue;
  }
  // 设置Cookie
  if (!getCookie("insur")) {
    if (!isSeo()) {
      setCookie("insur", setInsur(), 30);
    }
  }
  if (isSeo()) {
    if (!getCookie("insur")) {
      setCookie("insur", setInsur(), 30);
    } else {
      if (!getCookie("insur").includes("seo")) {
        setCookie("insur", setInsur(), 30);
      }
    }
  }
</script>

</html>